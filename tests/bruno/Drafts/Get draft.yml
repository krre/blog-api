info:
  name: Get draft
  type: http
  seq: 2

http:
  method: GET
  url: "{{host}}/drafts/{{post.id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
