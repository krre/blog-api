info:
  name: Hide post
  type: http
  seq: 7

http:
  method: PATCH
  url: "{{host}}/posts/{{post.id}}/hide"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
