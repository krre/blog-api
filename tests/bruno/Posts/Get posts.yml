info:
  name: Get posts
  type: http
  seq: 2

http:
  method: GET
  url: "{{host}}/posts?limit=20&offset=0"
  params:
    - name: limit
      value: "20"
      type: query
    - name: offset
      value: "0"
      type: query

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
