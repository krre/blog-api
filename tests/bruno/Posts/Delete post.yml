info:
  name: Delete post
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{host}}/posts/{{post.id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
