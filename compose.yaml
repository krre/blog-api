services:
  app:
    build:
      context: .
      target: final
    image: krre/blog-api:v1.0
    environment:
      - HOST=0.0.0.0
      - PORT=3000
      - RUST_LOG=info
    ports:
      - 4010:3000
